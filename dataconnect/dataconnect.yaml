specVersion: "v1beta"
serviceId: "take-a-seat-phone-app"
location: "europe-north1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "y-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
